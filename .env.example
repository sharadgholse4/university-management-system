# .env.example
# Template for environment variables.
# This file should be copied to .env and populated with real secrets in production.
# WHY: Storing secrets in environment variables (12-factor app methodology) prevents them from being hardcoded or committed to version control.

JWT_SECRET=purvansh-2026-sharad
JWT_EXPIRY_SECONDS=3600
DB_PATH=./university.db
SERVER_PORT=8080
BCRYPT_COST=12
